/**
 * @deprecated Import from `@/lib/emails` instead. Re-exports kept for existing imports.
 */
export {
  buildEmailTemplate,
  defaultUniqueKey,
  type EmailEventType,
  type EmailTemplate,
  type EmailTemplateContext,
  type EmailRecipientRole,
} from "@/lib/emails";
