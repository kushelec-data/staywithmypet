/** Optional MP4 — when present, lazy-loaded video replaces the CSS scene. */
export const DOG_STORY_VIDEO_SRC = "/animations/dog-story.mp4";

/** Static fallback if the video fails to load. */
export const DOG_STORY_FALLBACK_IMAGE_SRC = "/animations/boston-terrier-run-poster.webp";
