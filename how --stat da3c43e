[1mdiff --git a/src/lib/auth.ts b/src/lib/auth.ts[m
[1mindex 6b77611..057f3f3 100644[m
[1m--- a/src/lib/auth.ts[m
[1m+++ b/src/lib/auth.ts[m
[36m@@ -1,4 +1,4 @@[m
[31m-import { getAuthRedirectOrigin as resolveAuthRedirectOrigin } from "@/lib/site-url";[m
[32m+[m[32mimport { getSiteOrigin } from "@/lib/site-url";[m
 import { createClient } from "@/lib/supabase";[m
 [m
 export async function signOut() {[m
[36m@@ -9,7 +9,7 @@[m [mexport async function signOut() {[m
 [m
 /** Base URL for auth redirects (OAuth callback, password reset landing). */[m
 export function getAuthRedirectOrigin(): string {[m
[31m-  return resolveAuthRedirectOrigin();[m
[32m+[m[32m  return getSiteOrigin();[m
 }[m
 [m
 export function getAuthCallbackUrl(nextPath: string) {[m
[1mdiff --git a/src/lib/site-url.ts b/src/lib/site-url.ts[m
[1mindex 8bb29d3..4acea57 100644[m
[1m--- a/src/lib/site-url.ts[m
[1m+++ b/src/lib/site-url.ts[m
[36m@@ -1,74 +1,42 @@[m
 import { publicProfileHref } from "@/lib/profile-completeness";[m
 import { publicPetHref } from "@/lib/public-pet";[m
 [m
[31m-/** Last-resort production origin for server-rendered emails when env is unset. */[m
[31m-const FALLBACK_PRODUCTION_ORIGIN = "https://www.staywithmypet.ee";[m
[31m-[m
[31m-function normalizeOrigin(origin: string): string {[m
[31m-  return origin.trim().replace(/\/$/, "");[m
[31m-}[m
[31m-[m
[31m-/** Browser origin when running in the client (Preview, Production, or local). */[m
[31m-export function getClientOrigin(): string | null {[m
[31m-  if (typeof window === "undefined") return null;[m
[31m-  return normalizeOrigin(window.location.origin);[m
[31m-}[m
[31m-[m
[31m-/** Request origin for server routes (OAuth confirm/callback, API redirects). */[m
[31m-export function getRequestOrigin(request: Request | URL): string {[m
[31m-  const origin = request instanceof URL ? request.origin : new URL(request.url).origin;[m
[31m-  return normalizeOrigin(origin);[m
[31m-}[m
[31m-[m
[31m-/**[m
[31m- * Origin for Supabase auth redirects (OAuth, email confirm, password reset).[m
[31m- * Always uses the current browser origin on the client so Preview deployments[m
[31m- * do not send users to NEXT_PUBLIC_SITE_URL or a hardcoded production host.[m
[31m- */[m
[31m-export function getAuthRedirectOrigin(): string {[m
[31m-  const clientOrigin = getClientOrigin();[m
[31m-  if (clientOrigin) return clientOrigin;[m
[31m-[m
[31m-  if (process.env.VERCEL_ENV === "preview" && process.env.VERCEL_URL?.trim()) {[m
[31m-    return normalizeOrigin(`https://${process.env.VERCEL_URL.trim()}`);[m
[31m-  }[m
[31m-[m
[31m-  const fromEnv = process.env.NEXT_PUBLIC_SITE_URL?.trim();[m
[31m-  if (fromEnv) return normalizeOrigin(fromEnv);[m
[31m-[m
[31m-  if (process.env.NODE_ENV === "development") {[m
[31m-    return "http://localhost:3000";[m
[32m+[m[32m/** Canonical production origin for share links and emails when env is unset. */[m
[32m+[m[32mexport const DEFAULT_SITE_ORIGIN = "https://staywithmypet-clean.vercel.app";[m
[32m+[m
[32m+[m[32mfunction isLocalDevOrigin(origin: string): boolean {[m
[32m+[m[32m  try {[m
[32m+[m[32m    const { hostname } = new URL(origin);[m
[32m+[m[32m    return hostname === "localhost" || hostname === "127.0.0.1";[m
[32m+[m[32m  } catch {[m
[32m+[m[32m    return false;[m
   }[m
[31m-[m
[31m-  return FALLBACK_PRODUCTION_ORIGIN;[m
 }[m
 [m
 /**[m
[31m- * Canonical site origin for share links and emails.[m
[31m- * Prefers NEXT_PUBLIC_SITE_URL in production; uses current origin in the browser when unset.[m
[32m+[m[32m * Site origin for share links, emails, and auth redirects.[m
[32m+[m[32m * Prefers NEXT_PUBLIC_SITE_URL so preview deployments do not emit preview URLs.[m
  */[m
 export function getSiteOrigin(): string {[m
   const fromEnv = process.env.NEXT_PUBLIC_SITE_URL?.trim();[m
[31m-  if (fromEnv) return normalizeOrigin(fromEnv);[m
[31m-[m
[31m-  const clientOrigin = getClientOrigin();[m
[31m-  if (clientOrigin) return clientOrigin;[m
[32m+[m[32m  if (fromEnv) return fromEnv.replace(/\/$/, "");[m
 [m
[31m-  if (process.env.VERCEL_ENV === "production") {[m
[31m-    return FALLBACK_PRODUCTION_ORIGIN;[m
[32m+[m[32m  if (typeof window !== "undefined") {[m
[32m+[m[32m    const origin = window.location.origin.replace(/\/$/, "");[m
[32m+[m[32m    if (isLocalDevOrigin(origin)) return origin;[m
   }[m
 [m
[31m-  return "http://localhost:3000";[m
[32m+[m[32m  return DEFAULT_SITE_ORIGIN;[m
 }[m
 [m
 /** Absolute URL for a member's public profile page. */[m
 export function absolutePublicProfileUrl(profileId: string, origin?: string): string {[m
[31m-  const base = normalizeOrigin(origin ?? getSiteOrigin());[m
[32m+[m[32m  const base = (origin ?? getSiteOrigin()).replace(/\/$/, "");[m
   return `${base}${publicProfileHref(profileId)}`;[m
 }[m
 [m
 /** Absolute URL for a pet's public profile page. */[m
 export function absolutePublicPetUrl(petId: string, origin?: string): string {[m
[31m-  const base = normalizeOrigin(origin ?? getSiteOrigin());[m
[32m+[m[32m  const base = (origin ?? getSiteOrigin()).replace(/\/$/, "");[m
   return `${base}${publicPetHref(petId)}`;[m
 }[m
